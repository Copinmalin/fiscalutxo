{
  "name": "fiscalutxo",
  "version": "0.0.0",
  "private": true,
  "description": "Local-first Bitcoin-only tax preparation helper for French residents.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "keywords": [
    "bitcoin",
    "tax",
    "csv",
    "local-first",
    "france"
  ],
  "license": "UNLICENSED",
  "devDependencies": {
    "typescript": "^5.0.0",
    "vitest": "^3.0.0"
  }
}
